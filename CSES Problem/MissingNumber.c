#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int array[n]; freq[]
    for (int i = 1; i < n; i++)
    {
        scanf("%d", &array[i]);
    }
    for (int i = 1; i <= n; i++)
    {
        printf("%d ", array[i]);
    }
}